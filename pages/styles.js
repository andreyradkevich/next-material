export default function ({ palette }) {
  return {
    root: {
      background: palette.primary.light,
      height: '100vh',
      textAlign: 'center'
    },
    button: {
      marginRight: 10,
      background: 'transparent'
    }
  }
}
